import { Box } from "@chakra-ui/react";

const About: React.FC = () => {
  return <Box w={`100%`} left={0} top={`50%`} right={0} position={`fixed`} zIndex={1000} >ggggg</Box>;
};

export default About;
